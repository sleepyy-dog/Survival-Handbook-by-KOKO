# 数学实验与数学软件第四次作业

## 姓名：郑博引 

## 学号：23339147

### 作业：P85 2 P87 9 P72 15

> [!NOTE]
>
> 考试可能会考一个改进欧拉方法的计算代码编写。

> #### P85 **第2题**
>
> 用欧拉方法和龙格-库塔方法求下列微分方程初值问题的数值解，画出解的图形，对结果进行分析比较。
> $$
> (1)
> \begin{cases}
> y'=y+2x, \\
> y(0)=1
> \end{cases}
> (0 \le x \le 1), \text{精确解 } y=3e^x-2x-2;
> $$
> $$
> (2)
> \begin{cases}
> y'=x^2-y^2, \\
> y(0)=0 \text{ 或 } y(0)=1;
> \end{cases}
> $$
> $$
> (3)
> \begin{cases}
> x^2y''+xy'+(x^2-n^2)y=0, \\
> y\left(\frac{\pi}{2}\right)=2, y'\left(\frac{\pi}{2}\right)=-\frac{2}{\pi}
> \end{cases}
> (\text{贝塞尔方程}, \text{令 } n=0.5), \text{精确解 } y=\sin x \sqrt{\frac{2\pi}{x}}.
> $$

**解：**

三题代码如下：

```matlab
%% P85 2（1）
clc
function y=real(x)
y=3.*exp(x)-2.*x-2;
end

function f=sol(x,y)
f=y+2.*x;
end

X=0:0.05:1;

[X,Y1]=ode23(@sol,X,1);

Y2=ones(length(X),1);

Y2(1)=1;

for i=2:length(X)
Y2(i)=Y2(i-1)+0.05*sol(X(i-1),Y2(i-1));
end

Y=real(X);

figure; 
plot(X, Y, 'b-', 'LineWidth', 1.5); 
hold on; 
plot(X, Y1, 'r--', 'LineWidth', 1.5); 
plot(X, Y2, 'g--.', 'LineWidth', 1.5);
hold off; 
legend('Y = 3*exp(x).*-2.*x-2', '龙格塔库方法','欧拉方法');
xlabel('x');
ylabel('y');
grid on; 

%% P85 2（2）
function f=sol2(x,y)
f=x^2-y^2;
end

% 欧拉方法的函数
function Y2=euler(init,X)
Y2=ones(length(X),1);

Y2(1)=init;

h=X(2)-X(1);

for i=2:length(X)
Y2(i)=Y2(i-1)+h*sol2(X(i-1),Y2(i-1));
end

end

X=0:0.05:1;

[X1,Y1]=ode23(@sol2,X,0);
Y11=euler(0,X);
[X2,Y2]=ode23(@sol2,X,1);
Y21=euler(1,X);
figure; 
plot(X, Y1, 'b-', 'LineWidth', 1.5); 
hold on; 
plot(X, Y11, 'b--', 'LineWidth', 1.5);
plot(X1, Y2, 'r-', 'LineWidth', 1.5); 
plot(X1, Y21, 'r--', 'LineWidth', 1.5); 
hold off; 
legend('y(0)=0龙格塔库方法','y(0)=0欧拉方法', 'y(0)=1龙格塔库方法', 'y(0)=1欧拉方法');
xlabel('x');
ylabel('y');
grid on; 

%% P85 2（3）
clc
function dY=f(x,Y)
dY=ones(2,1);
dY(1)=Y(2);
y=Y(1);
y1=Y(2);
n=0.5;
dY(2)=((n^2-x^2)*y-x*y1)/(x^2);
end

% 欧拉方法的函数
function Y2=euler3(init,X)
Y2=ones(length(X),2);

Y2(1,:)=init;

h=X(2)-X(1);

for i=2:length(X)
Y2(i,:)=Y2(i-1,:)+h*(f(X(i-1),Y2(i-1,:)))';
end

end

function y=real3(x)
y=sin(x).*(2*pi./x).^(1/2);
end

X=pi/2:0.025*pi:pi;

Y0=[2;-2/pi];

Yreal=real3(X);

[X1,Y]=ode23(@f,X,Y0);

Y1=Y(:,1);

Y=euler3(Y0,X);

Y2=Y(:,1)

figure; 
plot(X, Yreal, 'b-', 'LineWidth', 1.5); 
hold on; 
plot(X1, Y1, 'r--', 'LineWidth', 1.5); 
plot(X1, Y2, 'g--', 'LineWidth', 1.5); 
hold off; 
legend('Y = sin(x).*(2*pi./x).^(1/2)', '龙格库塔方法 ','欧拉方法');
xlabel('x');
ylabel('y');
grid on; 
```

得到结果如图：

（1）

![image-20251003201815274](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202510032018466.png)

（2）

![image-20251003201827000](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202510032018130.png)

（3）

![image-20251003201835722](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202510032018867.png)

结论：我们ode方法得到的解和真实解是高度近似的，同时初值的改变对于解的结果影响是巨大的。

同时，对于以上三题，我们还知道

* 高阶数值方法（如龙格-库塔法）在求解常微分方程时，其精度显著优于低阶方法（如欧拉法）。
* 低阶方法（欧拉法）虽然计算简单，但其误差会随着积分步数的增加而累积，导致结果与真实解产生较大偏差。

> #### P85 第9题
>
> 9. 两种群相互竞争模型如下：
>
> $$
> \begin{cases}
> \dot{x}(t) = r_1 x \left(1 - \frac{x}{n_1} - s_1 \frac{y}{n_2}\right), \\
> \dot{y}(t) = r_2 y \left(1 - s_2 \frac{x}{n_1} - \frac{y}{n_2}\right)
> \end{cases}
> $$
>
> 其中 $x(t)$, $y(t)$ 分别为甲乙两种群的数量；$r_1, r_2$ 为它们的固有增长率；$n_1, n_2$ 为它们的最大容量。$s_1$ 的含义是，对于供养甲的资源而言，单位数量乙（相对 $n_2$）的消耗为单位数量甲（相对 $n_1$）消耗的 $s_1$ 倍，对 $s_2$ 可作相应的解释。
> 该模型无解析解，试用数值解法研究以下问题：
> (1) 设 $r_1=r_2=1, n_1=n_2=100, s_1=0.5, s_2=2$，初值 $x_0=y_0=10$，计算 $x(t), y(t)$，画出它们的图形及相图 $(x,y)$，说明时间 $t$ 充分大以后 $x(t), y(t)$ 的变化趋势（人们今天看到的已经是自然界长期演变的结局）。
> (2) 改变 $r_1, r_2, n_1, n_2, x_0, y_0$，但 $s_1, s_2$ 不变（或保持 $s_1<1, s_2>1$），计算并分析所得结果；若 $s_1=1.5(>1), s_2=0.7(<1)$，再分析结果。由此你能得到什么结论，请用各参数生态学上的含义作出解释。
> (3) 试验当 $s_1=0.8(<1), s_2=0.7(<1)$ 时会有什么结果；当 $s_1=1.5(>1), s_2=1.7(>1)$ 时又会有什么结果。能解释这些结果吗？

**解：**

三问代码如下：

```matlab
%% P87 9 (1)
clc

function dx=fx(x,y)
r1=1;
n1=100;
n2=100;
s1=0.5;
dx=r1.*x.*(1-x/n1-s1.*y/n2);
end

function dy=fy(x,y)
r2=1;
n1=100;
n2=100;
s2=2;
dy=r2.*y.*(1-s2.*x/n1-y/n2);
end

function dZ=f9(t,Z)
dZ=ones(2,1);
dZ(1)=fx(Z(1),Z(2));
dZ(2)=fy(Z(1),Z(2));
end

x0=10;
y0=10;
Z0=[x0;y0];

T=0:0.2:20;

[T1,Z1]=ode23(@f9,T,Z0);

X=Z1(:,1);
Y=Z1(:,2);

figure; 
plot(T1, X, 'b-', 'LineWidth', 1.5); 
hold on; 
plot(T1, Y, 'r--', 'LineWidth', 1.5); 
hold off; 
legend('甲种群', '乙种群');
xlabel('t');
ylabel('种群数量');
grid on; 

figure;
plot(X, Y), grid on;
xlabel('甲种群数量');
ylabel('乙种群数量');
set(gca, 'FontSize', 18);

%% P87 9 (2)
clc

function dx=fx2(x,y)
r1=2;
n1=100;
n2=100;
s1=1.5;
dx=r1.*x.*(1-x/n1-s1.*y/n2);
end

function dy=fy2(x,y)
r2=1;
n1=100;
n2=100;
s2=0.7;
dy=r2.*y.*(1-s2.*x/n1-y/n2);
end

function dZ=f92(t,Z)
dZ=ones(2,1);
dZ(1)=fx2(Z(1),Z(2));
dZ(2)=fy2(Z(1),Z(2));
end

x0=10;
y0=10;
Z0=[x0;y0];

T=0:0.2:20;

[T1,Z1]=ode23(@f92,T,Z0);

X=Z1(:,1);
Y=Z1(:,2);

figure; 
plot(T1, X, 'b-', 'LineWidth', 1.5); 
hold on; 
plot(T1, Y, 'r--', 'LineWidth', 1.5); 
hold off; 
legend('甲种群', '乙种群');
xlabel('t');
ylabel('种群数量');
grid on; 

figure;
plot(X, Y), grid on;
xlabel('甲种群数量');
ylabel('乙种群数量');
set(gca, 'FontSize', 18);

%% P87 9 (31)
clc

function dx=fx31(x,y)
r1=2;
n1=100;
n2=100;
s1=0.8;
dx=r1.*x.*(1-x/n1-s1.*y/n2);
end

function dy=fy31(x,y)
r2=1;
n1=100;
n2=100;
s2=0.7;
dy=r2.*y.*(1-s2.*x/n1-y/n2);
end

function dZ=f931(t,Z)
dZ=ones(2,1);
dZ(1)=fx31(Z(1),Z(2));
dZ(2)=fy31(Z(1),Z(2));
end

x0=10;
y0=10;
Z0=[x0;y0];

T=0:0.2:20;

[T1,Z1]=ode23(@f931,T,Z0);

X=Z1(:,1);
Y=Z1(:,2);

figure; 
plot(T1, X, 'b-', 'LineWidth', 1.5); 
hold on; 
plot(T1, Y, 'r--', 'LineWidth', 1.5); 
hold off; 
legend('甲种群', '乙种群');
xlabel('t');
ylabel('种群数量');
grid on; 

figure;
plot(X, Y), grid on;
xlabel('甲种群数量');
ylabel('乙种群数量');
set(gca, 'FontSize', 18);

%% P87 9 (32)
clc

function dx=fx32(x,y)
r1=2;
n1=100;
n2=100;
s1=1.5;
dx=r1.*x.*(1-x/n1-s1.*y/n2);
end

function dy=fy32(x,y)
r2=1;
n1=100;
n2=100;
s2=1.7;
dy=r2.*y.*(1-s2.*x/n1-y/n2);
end

function dZ=f932(t,Z)
dZ=ones(2,1);
dZ(1)=fx32(Z(1),Z(2));
dZ(2)=fy32(Z(1),Z(2));
end

x0=10;
y0=10;
Z0=[x0;y0];

T=0:0.2:20;

[T1,Z1]=ode23(@f932,T,Z0);

X=Z1(:,1);
Y=Z1(:,2);

figure; 
plot(T1, X, 'b-', 'LineWidth', 1.5); 
hold on; 
plot(T1, Y, 'r--', 'LineWidth', 1.5); 
hold off; 
legend('甲种群', '乙种群');
xlabel('t');
ylabel('种群数量');
grid on; 

figure;
plot(X, Y), grid on;
xlabel('甲种群数量');
ylabel('乙种群数量');
set(gca, 'FontSize', 18);
```

（1）

![image-20250929204104347](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202509292041422.png)

![image-20250929204113418](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202509292041486.png)

可以看出最后甲种群达到最大容纳量之后饱和不变，而乙种群则灭绝。

（2）

我们搜索相关参数知：

*   **内禀增长率 (r)**:
    *   r₁=2 表示在没有资源限制和竞争的理想条件下，甲种群的增长速度是乙种群（r₂=1）的两倍。甲种群具有更强的繁殖潜力。

*   **环境容纳量 (n)**:
    *   n₁=100, n₂=100 表示在没有对方种群的情况下，环境最多能分别支持100个单位的甲种群或乙种群。二者对环境基础资源的承载能力要求相同。

*   **竞争系数 (s)**:
    *   **s₁=1.5 (>1)**: 这是**乙对甲的竞争系数**。它的含义是：一个单位的乙种群个体对甲种群增长的抑制作用，是甲种群一个个体对自身种群增长抑制作用的1.5倍。换句话说，**种间竞争（来自乙）对甲的压力 > 种内竞争（来自甲自身）**。甲种群不仅要和同类抢资源，还要面对来自乙的更激烈的资源掠夺。
    *   **s₂=0.7 (<1)**: 这是**甲对乙的竞争系数**。它的含义是：一个单位的甲种群个体对乙种群增长的抑制作用，只有乙种群一个个体对自身种群增长抑制作用的0.7倍。换句话说，**种间竞争（来自甲）对乙的压力 < 种内竞争（来自乙自身）**。对于乙种群来说，来自同类的竞争比来自甲的竞争更激烈。

![image-20250929204256355](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202509292042427.png)

![image-20250929204320868](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202509292043947.png)

可以看到在s1 s2调整后，甲乙的角色互换，甲灭绝而乙饱和。

（3）1

![image-20250929204458606](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202509292044675.png)

![image-20250929204505374](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202509292045440.png)

（3）2

![image-20250929204547610](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202509292045679.png)

![image-20250929204554706](https://sleepy-dog-1376908035.cos.ap-guangzhou.myqcloud.com/202509292045777.png)

在第三问1的情形下，甲乙都没有灭绝，而是处在一个平衡情况，而2时，出现了二者竞争激烈，最后乙灭绝的情况。

我可以得到以下结论：

3.  **情况 (3.1): `s1=0.8 (<1), s2=0.7 (<1)` → 稳定共存**
    *   **分析**: 这是最关键的一种情况。`s1 < 1` 和 `s2 < 1` 同时成立，意味着**对于两个物种来说，种内竞争的制约作用都大于种间竞争**。通俗地说，每个物种的“内部矛盾”都大于“外部矛盾”。
    *   **结局**: 任何一个物种都无法将对方彻底消灭，因为当它数量增多时，会首先受到自身种内竞争的强烈限制，从而为对方留出生存空间。这导致两个种群最终达到一个**稳定的平衡点**，实现**长期共存**。从相图上看，轨迹线会盘旋进入一个位于第一象限内的稳定点。

4.  **情况 (3.2): `s1=1.5 (>1), s2=1.7 (>1)` → 不稳定平衡，结局依赖初值**
    *   **分析**: 这种情况与(3.1)相反，`s1 > 1` 和 `s2 > 1` 同时成立，意味着**两个物种的种间竞争都极其激烈，远大于它们各自的种内竞争**。这是一种“你死我活”的局面。
    *   **结局**: 理论上存在一个不稳定的平衡点，但任何微小的扰动都会打破这个平衡。最终的结局是**两种结局之一：要么甲胜出，要么乙胜出**。具体是哪种结局，**高度依赖于初始种群数量 `(x0, y0)` 和各自的增长率 `(r1, r2)`**。在你的模拟中（初值`x0=y0=10`，`r1=2, r2=1`），拥有更高增长率的甲种群占据了初期优势并最终胜出。如果改变初始条件，比如让乙的初始数量远大于甲，结果就可能反转。这被称为**双稳态（Bistability）**。

> #### **题目 (P72 15):**
>
> 验证改进欧拉方法($15$)式的精度。改进欧拉公式如下：
> $$
> \begin{cases}
> \bar{y}_{n+1} = y_n + hf(x_n, y_n) \\
> y_{n+1} = y_n + \frac{h}{2} [f(x_n, y_n) + f(x_{n+1}, \bar{y}_{n+1})]
> \end{cases}
> $$

**解：**

为验证改进欧拉方法的精度，我们需要计算其局部截断误差。考虑初值问题 $y' = f(x, y)$，假设在第 $n$ 步计算时是精确的，即数值解 $y_n$ 等于精确解 $y(x_n)$。

首先，将精确解 $y(x_{n+1})$ 在点 $x_n$ 处进行泰勒展开，有：
$$
y(x_{n+1}) = y(x_n) + h y'(x_n) + \frac{h^2}{2} y''(x_n) + \frac{h^3}{6} y'''(x_n) + O(h^4)
$$
其中 $y'(x) = f(x, y(x))$，$y''(x) = \frac{d}{dx}f(x, y(x)) = f_x + f_y f$。

接下来，分析数值解 $y_{n+1}$。将改进欧拉法的预测值 $\bar{y}_{n+1} = y_n + hf(x_n, y_n)$ 代入校正公式中，并对 $f(x_{n+1}, \bar{y}_{n+1})$ 项在点 $(x_n, y_n)$ 处进行二元泰勒展开：
$$
\begin{aligned}
f(x_{n+1}, \bar{y}_{n+1}) &= f(x_n + h, y_n + hf(x_n, y_n)) \\
&= f(x_n, y_n) + h \frac{\partial f}{\partial x}\bigg|_{(x_n, y_n)} + hf(x_n, y_n) \frac{\partial f}{\partial y}\bigg|_{(x_n, y_n)} + O(h^2) \\
&= f(x_n, y_n) + h(f_x + f \cdot f_y)\bigg|_{(x_n, y_n)} + O(h^2) \\
&= y'(x_n) + h y''(x_n) + O(h^2)
\end{aligned}
$$
将上式代入改进欧拉公式的校正公式中，得到 $y_{n+1}$ 的表达式：
$$
\begin{aligned}
y_{n+1} &= y_n + \frac{h}{2} [f(x_n, y_n) + f(x_{n+1}, \bar{y}_{n+1})] \\
&= y(x_n) + \frac{h}{2} [y'(x_n) + (y'(x_n) + h y''(x_n) + O(h^2))] \\
&= y(x_n) + \frac{h}{2} [2y'(x_n) + h y''(x_n) + O(h^2)] \\
&= y(x_n) + h y'(x_n) + \frac{h^2}{2} y''(x_n) + O(h^3) 
\end{aligned}
$$
方法的局部截断误差 $T_{n+1}$ 是精确解 $y(x_{n+1})$ 与数值解 $y_{n+1}$ 之差。联立式 $(2)$ 和式 $(4)$ 可得：
$$
T_{n+1} = y(x_{n+1}) - y_{n+1} = \left( \frac{h^3}{6} y'''(x_n) + O(h^4) \right) - O(h^3) = O(h^3)
$$
根据数值方法的精度定义，若局部截断误差为 $O(h^{p+1})$，则该方法具有 $p$ 阶精度。
在此问题中，局部截断误差为 $O(h^3)$，因此 $p+1=3$，解得 $p=2$。
所以，改进欧拉方法具有 $2$ 阶精度。

**证毕。**